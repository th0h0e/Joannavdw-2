// Shared project title styling used across ProjectTitle, ProjectIndex, and HamburgerMenu components
export const projectTitleClasses = "text-xl sm:text-2xl md:text-3xl lg:text-4xl xl:text-5xl uppercase leading-none block";

export const projectTitleStyle = {
  fontFamily: 'EnduroWeb, sans-serif',
  letterSpacing: '0.03em'
};

// Shared navigation container styling for ProjectIndex and HamburgerMenu
export const navigationContainerClasses = "w-full md:w-4/5 text-center px-6 md:px-0";

// Shared navigation link styling
export const navigationLinkClasses = "text-black text-xl sm:text-2xl md:text-3xl lg:text-4xl xl:text-5xl uppercase leading-none block no-underline hover:underline transition-all duration-200";

// Shared navigation list spacing
export const navigationListClasses = "space-y-1";

// Shared project title container padding (used in both navigation and carousel titles)
export const projectTitleContainerClasses = "px-6 md:px-0";